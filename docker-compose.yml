version: '3.8'

services:
  pdd-dashboard:
    build: .
    container_name: pdd-dashboard
    restart: always
    network_mode: host
    environment:
      - MYSQL_HOST=192.168.16.38
      - MYSQL_PORT=3306
      - MYSQL_USER=user1
      - MYSQL_PASSWORD=123456
      - MYSQL_DATABASE=pdd rating
